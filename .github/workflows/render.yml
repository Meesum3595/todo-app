# .github/workflows/render.yml
name: Render CI/CD Pipeline

# Trigger this workflow on pushes to the feature/render branch
on:
  push:
    branches:
      - feature/render

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Check out the repository
      - name: Check out repository
        uses: actions/checkout@v3

      # 2. Set up Node.js (adjust version as needed)
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      # 3. Install dependencies & build
      - name: Install dependencies & build
        run: |
          npm ci
          npm run build

      # 4. Install the Render CLI
      - name: Install Render CLI
        run: npm install -g @renderinc/cli

      # 5. Deploy to Render
      - name: Deploy to Render
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
        run: |
          render login --api-key $RENDER_API_KEY
          render services deploy srv-d0f9ih6mcj7s73d70uag

      # 6. Tail recent Render logs
      - name: Tail Render logs
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
        run: |
          render login --api-key $RENDER_API_KEY
          render logs srv-d0f9ih6mcj7s73d70uag --tail --since 5m

      # 7. Roll back on failure
      - name: Roll back on failure
        if: failure()
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
        run: |
          render login --api-key $RENDER_API_KEY
          render services rollback srv-d0f9ih6mcj7s73d70uag --to previous
